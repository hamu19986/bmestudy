/* ============================================================
   ENGINEERING GRAPHICS & DESIGN — TOPICS             [CONTENT PENDING]
   ------------------------------------------------------------
   Fill with EGD_UNIT1_TOPICS … EGD_UNIT4_TOPICS.
   Schema per topic (match js/data/bme/unit1.js objects):
   { id:'g2-lines', course:'egd', unit:2, category:'Projection of Lines',
     title:'…', summary:'…', overview:`…`, working:`…` (drawing procedure),
     parts:null, types:null, operations:null,
     formulas:[{formula, meaning, units, condition}],   // construction rules
     diagram:{description:'What to draw on the sheet.', svg:null},
     examTip:`…`, commonMistake:`…`, quickCheck:[{q,a}],
     applications:null, extraNotes:null }
   Unit ids: g1-* (basics/conics/scales), g2-* (points & lines),
             g3-* (planes & solids), g4-* (sections/development/isometric/CAD).
   See js/data/syllabus.js → SYLLABUS.egd for exact topic ids.
   ============================================================ */

const EGD_UNIT1_TOPICS = [
  // g1-drawing-basics, g1-conics, g1-scales
];

const EGD_UNIT2_TOPICS = [
  // g2-projection-basics, g2-points, g2-lines
];

const EGD_UNIT3_TOPICS = [
  // g3-planes, g3-solids-simple, g3-solids-inclined
];

const EGD_UNIT4_TOPICS = [
  // g4-sections, g4-development, g4-isometric, g4-cad-bim
];
