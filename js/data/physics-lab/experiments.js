/* ============================================================
   PHYSICS LAB — EXPERIMENTS  (25LC-PHY-103H)         [CONTENT PENDING]
   ------------------------------------------------------------
   Fill with PHYLAB_EXPERIMENTS1 … PHYLAB_EXPERIMENTS4
   (unit = section; labs keep the same topic schema).
   Schema per experiment (topic schema + lab extras):
   { id:'lab-phy-hall', course:'physics-lab', unit:2, category:'Section B — Electronics',
     title:'Experiment 5 — Hall effect',
     summary:'…',
     overview:`Aim + apparatus + principle.`,
     working:`Formula → procedure → observation table → calculation →
              graph → result → precautions → sources of error.`,
     parts:null, types:null, operations:null,
     formulas:[{formula, meaning, units, condition}],
     diagram:{description:'Circuit / ray diagram to draw.', svg:null},
     examTip:`Viva focus: least count, formula derivation.`,
     commonMistake:`Typical reading/calculation errors.`,
     quickCheck:[{q:'Viva question', a:'Expected answer'}],
     applications:null, extraNotes:null }
   Unit ids: lab-phy-* (see syllabus.js → SYLLABUS['physics-lab']).
   ============================================================ */

const PHYLAB_EXPERIMENTS1 = [
  // lab-phy-vernier, lab-phy-screw-gauge, lab-phy-spherometer, lab-phy-multimeter
];

const PHYLAB_EXPERIMENTS2 = [
  // lab-phy-hall, lab-phy-inverse-square, lab-phy-planck, lab-phy-solar-cell,
  // lab-phy-pn-diode, lab-phy-zener
];

const PHYLAB_EXPERIMENTS3 = [
  // lab-phy-bh-curve, lab-phy-curie, lab-phy-pob, lab-phy-coil-field,
  // lab-phy-capacitance-ne, lab-phy-de-sauty
];

const PHYLAB_EXPERIMENTS4 = [
  // lab-phy-sonometer, lab-phy-em, lab-phy-griffith
];
