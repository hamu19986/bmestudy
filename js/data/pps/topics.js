/* ============================================================
   PPS (PROGRAMMING FOR PROBLEM SOLVING) — TOPICS     [CONTENT PENDING]
   ------------------------------------------------------------
   Fill with PPS_UNIT1_TOPICS … PPS_UNIT4_TOPICS.
   Schema per topic (match js/data/bme/unit1.js objects):
   { id:'c3-arrays', course:'pps', unit:3, category:'Arrays & Strings',
     title:'…', summary:'…', overview:`…`, working:`…`,
     parts:null, types:null, operations:null,
     formulas:[{formula, meaning, units, condition}],   // syntax patterns
     diagram:{description:'…', svg:null},
     examTip:`…`, commonMistake:`…`, quickCheck:[{q,a}],
     applications:null, extraNotes:null }
   Unit ids: c1-* (computer fundamentals), c2-* (problem solving),
             c3-* (C fundamentals), c4-* (advanced C).
   See js/data/syllabus.js → SYLLABUS.pps for exact topic ids.
   ============================================================ */

const PPS_UNIT1_TOPICS = [
  // c1-functional-units, c1-classification, c1-memory, c1-software,
  // c1-number-systems, c1-codes
];

const PPS_UNIT2_TOPICS = [
  // c2-problem-steps, c2-algorithms, c2-flowcharts, c2-pseudocode,
  // c2-translators, c2-compilation
];

const PPS_UNIT3_TOPICS = [
  // c3-c-intro, c3-tokens, c3-data-types, c3-operators, c3-decisions,
  // c3-loops, c3-arrays, c3-strings, c3-search-sort
];

const PPS_UNIT4_TOPICS = [
  // c4-functions, c4-io, c4-preprocessor, c4-storage-classes, c4-recursion,
  // c4-structures, c4-pointers, c4-files, c4-dynamic-memory, c4-complexity
];
